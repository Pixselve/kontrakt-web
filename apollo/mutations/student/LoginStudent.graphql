mutation LoginStudent($username: Int!) {
  loginStudent(username: $username) {
    token
    student {
      id
      firstName
      lastName
      skillToStudents {
        mark
        skill {
          id
        }
      }
    }
  }
}
