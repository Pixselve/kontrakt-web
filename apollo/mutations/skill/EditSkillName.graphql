mutation EditSkillName($id: Int!, $name: String!) {
  updateSkill(where: {id: $id}, data: {name: $name}) {
    contract {
      start
      id
      skills {
        id
        name
        skillToStudents {
          mark {
            text
            value
            rgb
          }
        }
      }
    }
  }
}
