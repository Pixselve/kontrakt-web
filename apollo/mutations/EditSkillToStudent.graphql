mutation EditSkillToStudent(
  $markValue: String!
  $skillId: Int!
  $studentId: Int!
) {
  upsertOneSkillToStudent(
    skillToStudent: {
      markValue: $markValue
      studentId: $studentId
      skillId: $skillId
    }
  ) {
    mark {
      text
      value
    }
  }
}
