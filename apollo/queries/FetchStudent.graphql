query FetchStudent($id: Int!) {
  student(where: {id: $id}) {
    id
    firstName
    lastName
    skillsToStudent {
      mark {
        value
        text
        rgb
      }
      skill {
        id
      }
    }
  }
}
