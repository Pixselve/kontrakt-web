query FetchStudent($id: String!) {
  student(ownerUsername: $id) {
    ownerUsername
    firstName
    lastName
    groups {
      id
      name
    }
    studentSkills {
      mark
      skill {
        id
      }
    }
  }
}
