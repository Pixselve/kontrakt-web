query ContractByDate($date: DateTime!) {
  contracts(where: {date: {equals: $date}}) {
    date
    end
    id
    name
    skills {
      id
      name
      skillToStudents {
        id
        mark
      }
    }
  }
}
