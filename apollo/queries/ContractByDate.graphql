query ContractByDate($date: DateTime!) {
  contracts(where: {start: {equals: $date}}) {
    start
    end
    id
    name
    skills {
      id
      name
      skillToStudents {
        skillId
        studentId
        mark {
          rgb
          text
          value
        }
      }
    }
  }
}
