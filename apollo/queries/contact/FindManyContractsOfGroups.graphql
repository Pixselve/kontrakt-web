query FindManyContractsOfGroups($groups: [Int!]!) {
  contracts(groupIds: $groups) {
    id
    start
    end
    name
    hexColor
    groups {
      id
      name
    }
    skills {
      id
      name
    }
  }
}
