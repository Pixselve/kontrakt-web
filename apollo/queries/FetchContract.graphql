query FetchContract($id: Int!) {
  contract(where: {id: $id}) {
    id
    date
    end
    name
    skills {
      id
      name
      skillToStudents {
        id
        mark
        student {
          id
          firstName
          lastName
        }
      }
    }
  }
}
