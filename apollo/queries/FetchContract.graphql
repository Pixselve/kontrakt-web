query FetchContract($id: Int!) {
  contract(id: $id) {
    id
    start
    end
    name
    hexColor
    groups {
      id
      name
    }
    skills {
      id
      name
      studentSkills {
        mark
        student {
          ownerUsername
          firstName
          lastName
        }
      }
    }
  }
}

