query FetchContract($id: Int!) {
  contract(where: {id: $id}) {
    id
    start
    end
    name
    skills {
      id
      name
      skillToStudents {
        mark {
          text
          value
          rgb
        }
        student {
          id
          firstName
          lastName
        }
      }
    }
  }
}
